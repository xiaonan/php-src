// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("arr", "for arr support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("arr", "enable arr support", "no");

if (PHP_ARR != "no") {
	EXTENSION("arr", "arr.c");
}

